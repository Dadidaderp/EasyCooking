<?php

/*
 * Class Static qui enregistre toutes les informations du site.
 */
class Configuration {

    /* Type database. */
    public static $DATABASE_TYPE        = 'mysql';

    /* Adresse database. */
    public static $DATABASE_HOST        = 'localhost';

    /* Nom de la database. */
    public static $DATABASE_NAME        = 'EasyCooking';

    /* Login de la database. */
    public static $DATABASE_LOGIN       = 'root';

    /* Password de la database. */
    public static $DATABASE_PASSWORD    = 'root';

    /* Titre website. */
    public static $APPLICATION_TITLE    = 'Easy Cooking';

    /* URL  website. */
    public static $APPLICATION_URL      = 'http://127.0.0.1/AstroCompare/';
}
?>
